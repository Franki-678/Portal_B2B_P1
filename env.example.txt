# ============================================================
# Portal B2B Prototipo 1.1 — Variables de entorno
# ============================================================
# Copiá el contenido de este archivo a un nuevo archivo llamado .env.local y completalo con tus valores.
# NUNCA subas .env.local al repositorio (ya está en .gitignore).
# ============================================================

# ─── SUPABASE (requerido para conectar BD real) ─────────────
# Encontrás estos valores en: Settings > API de tu proyecto Supabase

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key-here

# ─── NOTA SOBRE SEGURIDAD ───────────────────────────────────
# - NEXT_PUBLIC_* = visible en el cliente (OK para anon key)
# - La service_role key NUNCA debe usarse en frontend
# - Si necesitás la service_role key para features server-only,
#   agregarla SIN el prefijo NEXT_PUBLIC_:
#
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key  ← solo server

# ─── MODO SIN SUPABASE ──────────────────────────────────────
# Si no configurás estas variables, la app usa mock data local.
# Todo el flujo funciona igual pero sin persistencia.
